<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="una.moviles.ui.home.HomeFragment"
        android:label="Avaliable flights"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/nav_purchase"
        android:name="una.moviles.ui.purchase.PurchaseFragment"
        android:label="Purchases"
        tools:layout="Purchases" />

    <fragment
        android:id="@+id/nav_profile"
        android:name="una.moviles.ui.profile.ProfileFragment"
        android:label="Profile"
        tools:layout="Profile" />
    <fragment
        android:id="@+id/nav_check"
        android:name="una.moviles.ui.checkout.CheckoutFragment"
        android:label="fragment_checkout"
        tools:layout="@layout/fragment_checkout" />
</navigation>